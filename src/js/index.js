import "../scss/index.scss";

import Glide from "@glidejs/glide";

  new Glide(".glide", {
    type: "slider",
    // autoplay: 2000,
    animationDuration: 0,
    // animationDuration: 600,
    hoverpause: false,
    gap: 0,
  }).mount();

